// const { Telegraf, session, Scenes, Markup,} = require("telegraf");
// require("dotenv").config();
// const bot = new Telegraf(process.env.BOT_TOKEN);


// bot.action(["yes", "no"], async(ctx) => ctx.scene.enter("threeWizard"));

// // Export the bot instance
// module.exports = bot;
